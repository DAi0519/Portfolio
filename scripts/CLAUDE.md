# scripts/
> L2 | 父级: ../CLAUDE.md

成员清单

convert_audio.js: 使用 ffmpeg-static 将 public/musics 下的 WAV 批量转换为 MP3
migration_draft.ts: 历史数据库迁移草稿，保留内容列迁移的探索记录
sync-notion.ts: 本地与 GitHub Actions 同步入口，加载可选本地环境后调用统一运行时

[PROTOCOL]: 变更时更新此头部，然后检查 CLAUDE.md
